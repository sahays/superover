# Core Framework
fastapi==0.115.5
uvicorn[standard]==0.34.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.20

# GCP Libraries
# google-auth <2.55 crashes in the GCE-metadata fallback ('Request' object
# has no attribute 'session') when no ADC file is present.
google-auth>=2.55.0
google-cloud-storage==2.19.0
# >=2.21 required: 2.20 caps protobuf <6 which conflicts with
# google-cloud-bigtable >=2.40 (needs protobuf >=6.33)
google-cloud-firestore==2.28.0
google-genai>=1.65.0

# Video Processing (Transcoder API)
google-cloud-video-transcoder>=1.0.0

# Speech-to-Text (Chirp 3)
google-cloud-speech>=2.25.0

# BigQuery (Natural Language Search, search_backend=bigquery)
google-cloud-bigquery>=3.25.0

# Bigtable (KNN vector search, search_backend=bigtable)
google-cloud-bigtable>=2.40.0

# Utilities
python-dotenv==1.0.1
aiofiles==24.1.0

# WebSocket client (Vertex AI Live upstream connection in avatars_live.py)
websockets>=12.0

# Development
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
